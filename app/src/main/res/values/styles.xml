<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="actionBarStyle">@style/actionBarStyle</item>
        <item name="colorPrimaryDark">@color/statusBar</item>
        <item name="android:textColorTertiary">@color/textDarkPrimary</item>
    </style>

    <style name="actionBarStyle" parent="Widget.AppCompat.Light.ActionBar">
        <item name="height">40dip</item>
        <item name="background">@color/actionBarBackground</item>
        <item name="titleTextStyle">@style/actionBarTitleStyle</item>
    </style>

    <style name="actionBarTitleStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/textDarkPrimary</item>
    </style>

    <drawable name="transparameter">#bb000000</drawable>

    <style name="Theme.TranslucentBackground" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBarOverlay">true</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowBackground">@drawable/transparameter</item>
    </style>

</resources>